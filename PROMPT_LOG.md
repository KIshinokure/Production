# Лог проектирования распределенной системы с помощью ИИ (PROMPT_LOG.md)

**Студент:** Дикарев Артём  
**Группа:** 221131  
**Вариант:** 16 (Повышенная сложность)

## Сессия 1: Проектирование сетевого взаимодействия и брокера сообщений
* **Запрос:** "Создай топологию сети на NATS для 4 распределенных агентов (dispatcher, inventory, planner, quality), работающих в парадигме аукционного распределения задач. Напиши конфигурацию docker-compose."
* **Результат:** Сформирована конфигурация топиков `auction.*` и `task.*`. Настроены порты для ОС Windows (4223).

## Сессия 2: Инструментация кода и распределенная трассировка (Jaeger + OpenTelemetry)
* **Запрос:** "Добавь в Go-код распределенных агентов трассировку OpenTelemetry с отправкой спанов в Jaeger по протоколу gRPC. Реализуй персистентность состояния счетчиков задач внутри Redis."
* **Результат:** Код агентов модифицирован. Интегрированы пакеты `go.opentelemetry.io/otel`. Реализовано сохранение и автоматическое восстановление состояния агента после рестарта из хэш-таблиц Redis.

## Сессия 3: Реализация алгоритма аукциона и LLM-агента
* **Запрос:** "Напиши код Python-оркестратора, реализующий выбор исполнителя с наименьшей стоимостью выполнения (динамический аукцион), а также добавь вызов локальной модели Ollama (Llama3) для предиктивного анализа цепочки."
* **Результат:** Написан `orchestrator.py`, включающий стейт-машину, Docker SDK для масштабирования контейнеров при росте очередей и интеграцию с LLM.

## Сессия 4: Разработка Web-UI
* **Запрос:** "Напиши реактивный интерфейс на Streamlit для визуализации состояния очередей из Redis."
* **Результат:** Разработан интерфейс `ui/app.py`.
